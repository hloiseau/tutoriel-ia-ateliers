Pour passer en mode `marker` nous allons utiliser la commande `:set foldmethod=marker`.

Cette méthode utilise les marqueurs `{{{` et `}}}` pour délimiter le début et la fin d'un pli.

L'avantage de cette méthode est que les plis sont persistants lorsque vous fermez le fichier.