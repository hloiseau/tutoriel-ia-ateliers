Cette méthode de pliage est automatiquement utilisée lorsque l'on utilise Vim en mode *diff* (avec la commande `vimdiff` ou `vim -d`).

Le principe est simple. Toutes les lignes identiques aux deux fichiers ouverts sont pliées tandis que les lignes différentes ne le sont pas.