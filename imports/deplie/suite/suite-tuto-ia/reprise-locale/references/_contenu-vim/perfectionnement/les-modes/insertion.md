Le mode insertion est celui qui permet d'insérer du texte pour éditer le fichier ouvert.

Pour passer du mode normal au mode insertion on utilise, par exemple, la commande `i`. Pour quitter et retourner en mode normal on presse la touche `Esc`.