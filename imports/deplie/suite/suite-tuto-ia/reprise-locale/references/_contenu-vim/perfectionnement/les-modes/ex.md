Nous avons vu le mode ligne de commande. Cependant après chaque commande nous retournons en mode normal. Le mode ex permet de continuer à taper des commandes les unes après les autres.

Pour entrer en mode ex il faut taper `Q` (`Maj + q`) et pour le quitter taper la commande `vi` ou `visual`.